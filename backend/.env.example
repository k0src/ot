# Backend environment variables (NON-SECRET PLACEHOLDERS ONLY).
# Copy to backend/.env for local development. See root .env.example for the
# full annotated list. Outbound Azure calls use managed identity, not keys.

NODE_ENV=development
PORT=3000

# Database (local dev uses Azure SQL Edge in Docker; dev-only password).
DATABASE_URL=sqlserver://localhost:1433;database=oti;user=sa;password=dev-password123;encrypt=true;trustServerCertificate=true

# Entra ID — JWT validation
ENTRA_TENANT_ID=00000000-0000-0000-0000-000000000000
ENTRA_API_CLIENT_ID=00000000-0000-0000-0000-000000000000
ENTRA_API_AUDIENCE=api://oti-workspace

# Azure OpenAI / AI Foundry
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_API_VERSION=2024-10-21
AZURE_OPENAI_DEPLOYMENT_GPT5=gpt-5
AZURE_OPENAI_DEPLOYMENT_GPT5_MINI=gpt-5-mini
AZURE_OPENAI_DEPLOYMENT_EMBEDDINGS=text-embedding-3-large
AZURE_AI_PROJECT_ENDPOINT=https://your-project.services.ai.azure.com

# Azure AI Search
AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
AZURE_SEARCH_INDEX_NAME=oti-evidence

# Azure Blob Storage
AZURE_STORAGE_ACCOUNT_URL=https://yourstorage.blob.core.windows.net
AZURE_STORAGE_CONTAINER=evidence

# Azure Key Vault
AZURE_KEY_VAULT_URL=https://your-kv.vault.azure.net

# Application Insights
APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=00000000-0000-0000-0000-000000000000

# Azure DevOps (read-only)
ADO_ORG_URL=https://dev.azure.com/your-org
ADO_PROJECT=your-project
